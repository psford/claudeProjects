# Bluesky Feed Filter - Environment Variables
# Copy this to .env and fill in the values

# Your Bluesky handle (without @)
BLUESKY_HANDLE=psford.com

# App password from https://bsky.app/settings/app-passwords
# Create one named "feed-generator" — DO NOT use your main password
BLUESKY_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# Hostname where this service is reachable (for DID document)
HOSTNAME=bsky-feed.psford.com

# Cloudflare Tunnel token (from Zero Trust dashboard)
CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token-here

# Optional overrides (defaults are fine)
# PORT=3000
# SELF_REPOST_MAX_AGE_HOURS=24
# FOLLOW_REFRESH_INTERVAL_SECONDS=3600
# DB_RETENTION_HOURS=48
